#!/bin/bash
echo "If external disks are connected it will run much slower!"
cat exclude.txt | grep . | sed 's/^/\\s/' > /tmp/diff_exclude.txt
sudo find / -type f -size +100k ! -iwholename "/Volumes*" -exec du -akx {} \; | grep -vf /tmp/diff_exclude.txt > snapshot.txt
